<!-- <br/>
<div class="col-md-4 col-centered">
  <div class="panel panel-default">
    <div class="panel-heading">
      <h3>投递><%= @job.title %></h2>
    </div>
    <div class="panel-body">
      <p><%= simple_format(@job.description)%></p>
      <h3>薪资：<%= @job.wage_lower_bound %>-<%= @job.wage_upper_bound %></h2>
      <h3>发布时间：<%= @job.created_at.strftime("%Y-%m-%d") %></h2>
      <h3><%= link_to("提交简历", new_job_resume_path(@job), class:"btn btn-default btn-lg") %></h2>
    </div>
  </div>
</div> -->
